notice("My ip address is ${facts['networking']['ip']}")
notice("My ip address is ${facts['networking']['hostname']}")
notice("My FQDN is ${facts['networking']['fqdn']}")
notice("My domain is ${facts['networking']['domain']}")
